source "https://rubygems.org"

# Use Jekyll 3.9.x which supports Ruby >= 2.4.0
gem "jekyll", "~> 3.9.0"

# Exclude webrick for compatibility, though webrick is built-in in Ruby 2.6.
# Exclude ffi version lock issues by letting bundler resolve older ffi if needed
gem "ffi", "~> 1.15.5" # FFI 1.15.5 supports Ruby >= 2.3
gem "kramdown-parser-gfm"

